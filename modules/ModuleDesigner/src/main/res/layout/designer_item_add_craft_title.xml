<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View" />

        <variable
            name="entity"
            type="com.deti.designer.materiel.popup.craft.item.title.ItemTitleCraftEntity" />

        <variable
            name="viewModel"
            type="com.deti.designer.materiel.popup.craft.AddCraftViewModel" />
    </data>

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="left"
        android:orientation="vertical">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tv_del"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:paddingStart="16mm"
            android:paddingTop="16mm"
            android:paddingEnd="16mm"
            android:paddingBottom="16mm"
            android:text="@{entity.title}"
            android:textColor="#333333" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1mm"
            android:background="#EEEEEE"
            android:visibility="@{entity.isShowLine == true ? View.VISIBLE: View.GONE}" />

    </androidx.appcompat.widget.LinearLayoutCompat>
</layout>