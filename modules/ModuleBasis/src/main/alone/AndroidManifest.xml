<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.deti.basis">

    <application
        android:name="com.test.common.CommonAppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/DetiAppTheme">
        <activity android:name="com.deti.basis.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.deti.basis.login.LoginActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name="com.deti.basis.register.RegisterAcitivty" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".personal.PersonalActivity" android:windowSoftInputMode="adjustResize"/>
    </application>

</manifest>